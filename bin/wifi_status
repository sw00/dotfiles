#!/usr/bin/env bash

if $(uname -a | grep -q microsoft); then
  STATS=$(powershell.exe -Command "(netsh wlan show interfaces) -Match '(Signal|Receive|Transmit)'" | tr -d '\r')
  echo $STATS | awk '{print "UP=" $10 " DOWN=" $5 " SIGSTR=" $13}'
fi
