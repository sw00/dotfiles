#!/bin/bash

ip_address_command () {
  OS=$(uname -s)

  if [[ $OS == "Linux" ]]; then
    ip address
  elif [[ $OS == "Darwin" ]]; then
    ifconfig
  fi
}

main () {
  ip_address_command | awk '$1 == "inet" { print "[" $2 "] " }' | tail -n +2 | tr -d '\n'
}

main
