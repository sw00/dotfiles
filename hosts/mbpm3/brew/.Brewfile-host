# ── Bootstrap / git ───────────────────────────────────────────────────────────
# Must exist before mise runs. stow/git-crypt bootstrap the dotfiles themselves.
brew "stow"
brew "git"
brew "git-crypt"
brew "git-lfs"
brew "gnupg"
brew "pinentry-mac"

# ── Shell ─────────────────────────────────────────────────────────────────────
# fish must be Homebrew-installed so it can be set as login shell via chsh.
brew "fish"

# ── Tool managers ─────────────────────────────────────────────────────────────
# pipx manages ~/.local/bin Python tools (aider, black, mypy, pre-commit, etc.).
# Homebrew-installed so it persists independently of mise Python versions.
brew "pipx"

# ── Terminal multiplexer ──────────────────────────────────────────────────────
# tmux is managed by mise (consistent version across all platforms).
# tmux-fingers bootstraps its own binary via TPM (<prefix>I); no tap needed.

# ── macOS-specific ────────────────────────────────────────────────────────────
# ── CLI tools not in mise/aqua ─────────────────────────────────────
brew "lf"        # terminal file manager (not in aqua registry)
brew "tig"       # text-mode git log/diff browser (not in aqua registry)


# ── Native compiled dependencies ──────────────────────────────────────────────
# Libraries that Python/other packages link against at the C level.
# mise provides cross-platform binaries but not .dylib / header files.
brew "libpq"     # PostgreSQL client lib (psycopg2, asyncpg, etc.)
brew "ffmpeg"    # Audio/video processing

# ── Tap-specific formulae ─────────────────────────────────────────────────────
# Not available in the aqua registry; must stay in Homebrew.
brew "boz/repo/kail"   # Kubernetes log tail

# ── Desktop apps ──────────────────────────────────────────────────────────────
cask "appcleaner"
cask "audacity"
cask "bruno"
cask "calibre"
cask "dbeaver-community"
cask "discord"
cask "docker"
cask "firefox@developer-edition"
cask "freelens"
cask "linear-linear"
cask "loom"
cask "macfuse"
cask "megasync"
cask "mounty"
cask "pycharm-ce"
cask "tomighty"
cask "vlc"
cask "vscodium"
cask "zotero"
cask "jan"

# ── VSCodium extensions ─────────────────────────────────────────────────────
# Canonical list: os/wsl/windows/vscodium/extensions.txt
# bootstrap.sh ensure_vscodium_extensions() installs from that file on all platforms.


# ── Mac App Store ─────────────────────────────────────────────────────────────
mas "Pixelmator", id: 407_963_104
