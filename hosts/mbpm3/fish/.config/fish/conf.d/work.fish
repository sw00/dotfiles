# work.fish — mbpm3-specific shell context
# Stowed by hosts/mbpm3/fish/; not sourced on other machines.
#
# For single-project env vars (API keys, feature flags), use direnv + .envrc
# in the project root instead of adding them here.

# Fly.io — default target for the lelapa workspace
abbr --add --global -- flyl 'fly -t lelapa'
