# Colima VM (macOS Docker daemon). Silently ignored on hosts without colima.
Include ~/.colima/ssh_config

Include ./config.d/git.conf
Include ./config.d/lan.conf
Include ./config.d/vps.conf
Include ./config.d/work.conf
# Work-specific: Include ./config.d/wtc.conf  (add manually on work machines)

# Host *
#    ControlMaster auto
#    ControlPath ~/.ssh/multiplex/%r@%h:%p
#    ControlPersist 3s
#    IdentitiesOnly yes
#    ForwardAgent yes
