# whkdrc — hotkey daemon config for komorebi
# Installed to %USERPROFILE%\.config\whkdrc by os/wsl/up.sh.
# See https://github.com/LGUG2Z/whkd for full syntax.
#
# whkd treats the right-hand side of `:` as a shell command, so every binding
# must invoke `komorebic` explicitly. Modifiers are joined with `+` (not `-`),
# and komorebic workspace indexes are 0-based (alt+1 → workspace 0).

.shell cmd

# Focus windows
alt + h             : komorebic focus left
alt + j             : komorebic focus down
alt + k             : komorebic focus up
alt + l             : komorebic focus right

# Move windows
alt + shift + h     : komorebic move left
alt + shift + j     : komorebic move down
alt + shift + k     : komorebic move up
alt + shift + l     : komorebic move right

# Workspace navigation (1-keyed, 0-indexed under the hood)
alt + 1             : komorebic focus-workspace 0
alt + 2             : komorebic focus-workspace 1
alt + 3             : komorebic focus-workspace 2
alt + 4             : komorebic focus-workspace 3
alt + 5             : komorebic focus-workspace 4
alt + 6             : komorebic focus-workspace 5
alt + 7             : komorebic focus-workspace 6
alt + 8             : komorebic focus-workspace 7
alt + 9             : komorebic focus-workspace 8
alt + 0             : komorebic focus-workspace 9

# Move windows across workspaces
alt + shift + 1     : komorebic move-to-workspace 0
alt + shift + 2     : komorebic move-to-workspace 1
alt + shift + 3     : komorebic move-to-workspace 2
alt + shift + 4     : komorebic move-to-workspace 3
alt + shift + 5     : komorebic move-to-workspace 4
alt + shift + 6     : komorebic move-to-workspace 5
alt + shift + 7     : komorebic move-to-workspace 6
alt + shift + 8     : komorebic move-to-workspace 7
alt + shift + 9     : komorebic move-to-workspace 8
alt + shift + 0     : komorebic move-to-workspace 9

# Workspace back-and-forth (aligned with AeroSpace alt-tab = workspace-back-and-forth)
alt + tab           : komorebic focus-last-workspace
alt + shift + tab   : komorebic cycle-monitor next

# Monitor focus (h/l for left/right — j/k removed, they duplicated h/l on a horizontal setup)
alt + ctrl + h      : komorebic cycle-monitor previous
alt + ctrl + l      : komorebic cycle-monitor next

# Move window to monitor (h = primary/0, l = secondary/1)
alt + shift + ctrl + h : komorebic move-to-monitor 0
alt + shift + ctrl + l : komorebic move-to-monitor 1

# Toggle modes
alt + f             : komorebic toggle-float
alt + m             : komorebic toggle-monocle
alt + t             : komorebic toggle-tiling

# Resize (aligned with Aerospace)
alt + -             : komorebic resize -50
alt + =             : komorebic resize +50
alt + shift + -     : komorebic resize -200
alt + shift + =     : komorebic resize +200
alt + shift + r     : komorebic resize-axis horizontal

# Layout
alt + x             : komorebic cycle-layout next
alt + shift + x     : komorebic cycle-layout previous
ctrl + alt + r      : komorebic reload-configuration

# Window operations
alt + q             : komorebic close
